server:
  port: 8081

spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    # ??? ??? ??? ???? ??
    url: # 사용할 DB URL 
    username: system
    password: oracle

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.springdocker.vo

